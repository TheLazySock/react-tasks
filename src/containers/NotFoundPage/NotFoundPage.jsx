import style from './style.scss';

const NotFoundPage = () => (
    <div className={ style['not-found-page'] }>
        <div className={ style['gradient-animation'] }>
            <h1>Not found</h1>
        </div>
    </div>
);

export default NotFoundPage;
